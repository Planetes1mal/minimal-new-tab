# Context — 极简新标签页

## Glossary

### 配色方案 (color scheme)
一组完整设计过的调色家族，覆盖全部 `--c-*` 颜色变量，并以**浅色/深色两个变体**成对提供。
当前值：`forest`（森绿，默认）、`ocean`（碧蓝）、`amber`（琥珀）、`slate`（岩墨）。
选择方案不改变页面几何，只改变颜色。

### 主题模式 (theme mode)
`light` / `dark` / `null`（跟随系统）三种模式之一，决定当前配色方案内使用哪个明暗变体。
与配色方案**正交**：切换主题模式不换方案，切换方案不换模式。
由角落的主题切换按钮操作，持久化在 `theme` 键。

### 语义色 (semantic colors)
表达状态含义的颜色：`danger`（危险/删除/错误）、`success`（成功/导入完成）及其强变体
（`danger-strong` / `success-strong`，用于浅色中性底上的状态文字）。
语义色是"状态语言"而非"品牌语言"，但**每套配色方案单独调色**以保证与方案底色和谐。

### 站点标记 (site marker)
快捷链接启动器中代表目标站点的圆点。已知站点使用品牌色（如微博红、知乎蓝），
未知站点按域名哈希从固定色板取色，同一域名永远同色。
站点标记是"站点身份"，**不随配色方案变化**。

### 时间格式 (hour format)
时钟的小时显示制式：`24`（默认）或 `12`。12 小时制输出 `2:35 PM`。
持久化在 `hourFormat` 键，与配色方案同为页面外观偏好。

### 极简表面 (minimal surface)
本页面的表面语言约定：**搜索框是唯一容器**（柔和胶囊），启动器与角落控制
均透明/扁平，靠留白与交互反馈（hover 淡底、聚焦环）建立层次，而非边框盒子。
静态状态无彩色，强调色只出现在语义/交互位置。

### 拖拽插入排序 (drag insertion)
启动器的排序交互语义：拖拽时在目标空隙显示 2px 强调色竖直插入线，
松手后链接**插入**到线所在位置，后续条目后移（insert），而非与相邻条目交换（swap）。

